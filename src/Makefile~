OBJECTS = ElmTypes.o ElmMethods.o

#FFLAGS = -g

FC = gfortran

.SUFFIXES: .f90 .o
.f90.o:
	$(FC) -c $<

all: test

# Husk at det skal være "tab" foran $(FC)
test: ${OBJECTS}
	$(FC) ${OBJECTS} -o Elm -lm

clean:
	rm Elm *.o *.mod *.Mod ${OBJECTS}
